import 'package:flutter/material.dart';

class NewsListTab extends StatelessWidget {
  const NewsListTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('news list');
  }
}
